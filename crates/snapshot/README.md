# knowledge-base-snapshot

Read-only, deterministic snapshots of canonical knowledge-base repository resources.

This crate deliberately performs structural loading only. Use `knowledge-base-validation` for generic semantic validation and composed domain validation.
