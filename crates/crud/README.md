# knowledge-base-crud

Typed filesystem access for a file-based knowledge base. The crate currently provides read operations and is structured to add create, update, and delete operations without coupling them to the command-line interface.
